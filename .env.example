# Optional production credentials. Copy this file to `.env.local` and restrict
# both tokens to your deployment origin. Without them, CesiumJS uses its bundled
# evaluation token while available and falls back to an OpenStreetMap globe if
# the photorealistic service cannot be reached.
VITE_GOOGLE_MAPS_API_KEY=
VITE_CESIUM_ION_TOKEN=
