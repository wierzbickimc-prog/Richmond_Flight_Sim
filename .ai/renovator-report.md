Fixed audit finding F1: rebound missile fire from KeyX to KeyV and documented it. Changes: (1) src/controls.js line 63 — `case 'KeyX':` → `case 'KeyV':` in the keydown switch; (2) index.html line 47 — added `<div><b>V</b> Fire Missile</div>` to the .panel-controls HUD panel after the G/B/Space row; (3) README.md line 62 — added `| V | Fire missile |` to the Controls table after the B / Space row. Verified: `node --check src/controls.js` passes, search confirms zero remaining `KeyX` references, and grep shows all three additions at the expected lines. No other files touched; missiles.js/main.js wiring was already correct per the plan.